ul.menu
    li
        a(href='/') Main
    if !user.login
        li
            a(href='/login') Sign In
    if user.login
        li
            a(href='/logout') Logout
        li.nickname
            | Hello,
            a(href='/profile') #{user.login}
